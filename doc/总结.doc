公共部分：
    1.头部
        -头部新闻滚动
        -登录切换状态

    2.搜索栏
        -搜索框
        -小型购物车

    3.导航
    4.侧边栏
        -登录切换状态
        -购物车总数
        -回到顶部
    5.尾部

页面：
    1.首页
        -轮播图
        -每日精选[降价最多的前六商品]淡入淡出轮播
        -生成列表 
            -根据数据显示商品状态
            -传参
        -懒加载

    2.美食民生（在售分类里）
        -读取美食类商品生成列表 
            -根据数据显示商品状态
            -传参
        -懒加载
        -上架时间、销量、价格排序
        -鼠标移入动画

    3.详情页
        -根据传参读取指定商品数据，部分信息判断显示
        -放大镜
        -左右滑动动画
        -数量加减与输入处理
        -飞入购物车
        -加入购物车 [登录加mysql 不登录加cookie]
        -无库存显示抢光、不能加入购物车
        -tab切换 [跳到对应top处]
        -滚动至详情后吸顶 [tab标签 商品数量 商品总价 加入购物车]
        -侧边（大家都在看）生成列表+传参

    4.购物车页
        -根据mysql/cookie生成列表
        -数量改变
        -删除
        -清空
        -当看不见支付时支付栏固定在下方

    5.登录
        -核对
        -记住用户名
    6.注册
        -查询
        -验证码
        -正则
        -写入
        -tab
        
    不总结了。

压缩后报错：require is not defined
算了，我放弃。